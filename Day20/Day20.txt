[ 자료구조 활용 추가문제2 - 과일 저장 ]
CRUD : create / read / update / read
if문 삼항 ? : 사용하기!

[ 자료구조 활용 추가문제3 - 사람 저장 ]
나중에 사람의 이름이 지어질 수 있어서
getter / setter로 하심!
toString은 object라서 출력이 맘에 안드면 @Override를 해야함
Person의 toString을 오버라이드하면 사람의 정보가 예쁘게 출력된다.

[ 자료구조 활용 추가문제4 - 홍당무 마켓 ]
Market이 list를 가지고 있어야 함
내가 요청.. class쓰고, list쓰고 등등

[ 쓰레드(Thread) ]
프로그램 - 명령어의 집합
프로세스 - 실행중인 프로그램
쓰레드 - 프로세스 개념과 거의 일치. 상속받기, Interface 구현하기
- 예) 매표소
데드락(deadlock) - 자원 분배가 적당히 되지 않아 프로그램이 종료됨
- 자원을 서로 무한정 기다리는 상태
1. 쓰레드 만들기 : Thread 클래스를 상속받아 사용한다.
단점: 자바는 단일상속이므로 Thread를 상속받으면 다른 클래스를 함께 상속 받을 수 없다.
Single Thread로 수행 -> 순차적 수행
Multi Thread: 각각의 쓰레드가 동시에 수행(start 메소드 사용)
출력되는 속도가 빨라서 싱글 쓰레드처럼 보이므로 sleep메소드 추가
2. 쓰레드 만들기: Runnable 인터페이스를 구현해서 사용한다.
상속도 사용가능하다.
단점: 멀티쓰레드를 위해서 Thread 객체에 담아서 사용한다.
